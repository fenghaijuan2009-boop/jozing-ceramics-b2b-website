import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { SiteFooter, SiteHeader } from "../../site-shell";
import { guides } from "../guide-data";
const origin="https://www.jozing.cn";
export function generateStaticParams(){return guides.map((guide)=>({slug:guide.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const guide=guides.find((item)=>item.slug===slug);if(!guide)return{};return{title:`${guide.title} | JOZING Buyer Guide`,description:guide.description,alternates:{canonical:`${origin}/guides/${slug}/`}}}
export default async function GuidePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const guide=guides.find((item)=>item.slug===slug);if(!guide)notFound();const url=`${origin}/guides/${slug}/`;const article={"@context":"https://schema.org","@type":"Article",headline:guide.title,description:guide.description,datePublished:guide.updated,dateModified:guide.updated,author:{"@type":"Organization",name:"JOZING Ceramics",url:origin},publisher:{"@type":"Organization",name:"Xiamen Jozing Industrial Co., Ltd.",url:origin,logo:{"@type":"ImageObject",url:`${origin}/jozing-logo.png`}},mainEntityOfPage:url};const breadcrumbs={"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:`${origin}/`},{"@type":"ListItem",position:2,name:"Buyer Guides",item:`${origin}/guides/`},{"@type":"ListItem",position:3,name:guide.title,item:url}]};return <main><SiteHeader/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(article)}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(breadcrumbs)}}/><article className="guide-article shell"><nav className="breadcrumbs"><a href="/">Home</a><span>/</span><a href="/guides">Buyer Guides</a></nav><p className="eyebrow">JOZING BUYER GUIDE</p><h1>{guide.title}</h1><p className="guide-deck">{guide.description}</p><div className="guide-meta">Updated {guide.updated}<span>·</span>{guide.readTime}<span>·</span>Reviewed by JOZING sourcing team</div>{guide.sections.map((section)=><section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map((text)=><p key={text}>{text}</p>)}{section.bullets&&<ul>{section.bullets.map((item)=><li key={item}>{item}</li>)}</ul>}</section>)}<aside><h2>Commercial details change by lot and market</h2><p>Always confirm current stock, packing, compliance scope, payment terms and shipment details for the actual order.</p><a className="btn primary" href="/contact">Request a quotation</a></aside></article><SiteFooter/></main>}
